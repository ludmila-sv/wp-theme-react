<?php
/**
 * It returns array of slugs of custom block patterns,
 * list them here in array, We don't rely on glob() because it's potential security flaw,
 * so we explicitly list patterns here.
 *
 * @package pressfoundry
 */

return array(
	'example',
);
